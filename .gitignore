# Secrets — never commit these
.env
.env.local
*.pem
*.key

# Tech lead's private notes. Not for the repo — it discusses the team by name.
HANDOFF.md

# The database is a file on your machine. It is yours, not the repo's.
# Delete it any time you want to start over with an empty app.
*.db
*.db-journal
# ...and any copy of one kept while migrating. A database renamed out of the way
# stops matching *.db and becomes an untracked file that git offers to commit.
*.db.*

# The embedding model downloads itself here the first time you run it (~130 MB)
.fastembed_cache/

# Uploaded notes
uploads/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
dist/
.vite/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
